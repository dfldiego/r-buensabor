import {
    ABRIR_MODAL,
} from '../types';

const initialState = {
    abrir__modal: false,
}

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}